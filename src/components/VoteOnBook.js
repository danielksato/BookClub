// @flow
import React, { PureComponent } from 'react';

export default class VoteOnBook extends PureComponent<$FlowFixMe> {
	static navString = 'Vote on Book';
	render() {
		return <div>Vote on Book</div>;
	}
}
